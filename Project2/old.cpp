/*
    do
    {
      std::getline(myFile, question);
      std::cout << question << "\n";
      std::getline(myFile, answer);
      //std::cin.ignore(2);
      std::cout << "Type in your answer: ";
      std::cin >>
      std::getline(std::cin, input);
      //std::cin.ignore();

      if(input == answer)
      {
        std::cout << "Correct!" << std::endl;
        Quiz.updateScore(s=s+1);
        std::cout << "Current score: " << Quiz.getScore() << std::endl;
      }
      else
      {
        std::cout << "Wrong! Correct answer: " << answer << std::endl;
        Quiz.updateScore(s=s-1);
        std::cout << "Current score: " << Quiz.getScore() << std::endl;
      }
      q=q+1;

    }while(std::getline(myFile >> question);


    std::cout << Result.printResult(q, s, q-s);
*/


//std::string line;
if(myFile.is_open())
{
int arrayLength = sizeof(argv[1]);
 for(int i=0; i<arrayLength; ++i)
 {

 }
